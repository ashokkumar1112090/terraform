# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
# Variables should be passed via other means, such as environment variables,
# a CI/CD system, or a secure secrets manager.
#
# If you must commit a tfvars file, use a specific name (e.g., terraform.tfvars.sample)
# and include the actual file name in this ignore list.
#
# example.tfvars
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
# and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc
